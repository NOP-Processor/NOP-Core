#include <bits/stdc++.h>

int main() {
    // generate a random number 1 or 0 get time
    srand(time(NULL));
    int random = rand() % 2;
    // sleep for 2 seconds
    // sleep(5);
    return random;
}